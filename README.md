# FlexiParser - Монорепозиторий с независимыми клиентом и сервером

## Архитектура

Проект разделен на два полностью независимых приложения:

### 📱 Клиент (React + TypeScript + Vite)
- Расположение: `/client`
- Запуск: `cd client && npm run dev`
- Порт: `3000`
- Сборка: Производит статические файлы

### 🖥️ Сервер (Node.js + Express + MongoDB)
- Расположение: `/server`
- Запуск: `cd server && npm run dev`
- Порт: `5000`
- Сборка: Производит серверный код

## Быстрый старт

### Вариант 1: Разработка (оба проекта)
```bash
# Клонировать репозиторий
git clone https://github.com/dasmindme/flexi-parser.git
cd flexi-parser

# Установить клиент
cd client && npm install

# Установить сервер
cd ../server && npm install

# Запустить оба (в разных терминалах)
# Терминал 1:
cd client && npm run dev

# Терминал 2:
cd server && npm run dev